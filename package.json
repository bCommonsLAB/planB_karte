{
  "name": "planb-karte",
  "version": "1.0.0",
  "description": "Interactive map for Plan B Brixen",
  "scripts": {
    "dev": "cd web && npm run dev",
    "build": "cd web && npm run build",
    "start": "cd web && npm run start"
  },
  "devDependencies": {
    "@tailwindcss/line-clamp": "^0.4.4",
    "@tailwindcss/postcss": "^4.1.5"
  }
}
